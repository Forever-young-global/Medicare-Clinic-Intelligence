import {sql} from "drizzle-orm";import {index,integer,sqliteTable,text} from "drizzle-orm/sqlite-core";
export const patients=sqliteTable("patients",{id:integer("id").primaryKey({autoIncrement:true}),fullName:text("full_name").notNull(),dateOfBirth:text("date_of_birth"),sex:text("sex"),phone:text("phone").notNull(),email:text("email"),address:text("address"),allergies:text("allergies").default(""),consentAt:text("consent_at").notNull(),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_patients_name").on(t.fullName),index("idx_patients_phone").on(t.phone)]);
export const appointments=sqliteTable("appointments",{id:integer("id").primaryKey({autoIncrement:true}),patientId:integer("patient_id").references(()=>patients.id),startsAt:text("starts_at").notNull(),visitType:text("visit_type").notNull().default("clinic"),reason:text("reason").default(""),status:text("status").notNull().default("confirmed"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_appointments_starts_at").on(t.startsAt),index("idx_appointments_patient").on(t.patientId)]);
export const consultations=sqliteTable("consultations",{id:integer("id").primaryKey({autoIncrement:true}),patientId:integer("patient_id").notNull().references(()=>patients.id),subjective:text("subjective").default(""),objective:text("objective").default(""),assessment:text("assessment").default(""),plan:text("plan").default(""),doctorDecision:text("doctor_decision").default(""),signedAt:text("signed_at"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const prescriptions=sqliteTable("prescriptions",{id:integer("id").primaryKey({autoIncrement:true}),consultationId:integer("consultation_id").references(()=>consultations.id),patientId:integer("patient_id").notNull().references(()=>patients.id),medication:text("medication").notNull(),directions:text("directions").notNull(),status:text("status").notNull().default("draft"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const reminders=sqliteTable("reminders",{id:integer("id").primaryKey({autoIncrement:true}),patientId:integer("patient_id").notNull().references(()=>patients.id),channel:text("channel").notNull().default("sms"),message:text("message").notNull(),dueAt:text("due_at").notNull(),status:text("status").notNull().default("queued")},t=>[index("idx_reminders_due_status").on(t.dueAt,t.status)]);
export const auditLogs=sqliteTable("audit_logs",{id:integer("id").primaryKey({autoIncrement:true}),actorId:text("actor_id").notNull(),action:text("action").notNull(),entityType:text("entity_type").notNull(),entityId:text("entity_id"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},t=>[index("idx_audit_created_at").on(t.createdAt)]);
